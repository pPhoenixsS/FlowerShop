services:
  apigateway:
    image: apigateway
    build:
      context: .
      dockerfile: ApiGateway/Dockerfile

  identity:
    image: identity
    build:
      context: .
      dockerfile: Identity/Dockerfile
